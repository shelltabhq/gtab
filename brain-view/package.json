{
  "name": "@gtab/brain-view",
  "version": "0.1.0",
  "description": "Portable GTab Brain View tile — Svelte 5 SPA + Bun server, runs against any gbrain corpus.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "bun run server/server.ts",
    "start": "bun run server/server.ts",
    "typecheck": "bunx tsc --noEmit && bunx svelte-check"
  },
  "dependencies": {
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "@tsconfig/svelte": "^5.0.4",
    "bun-types": "^1.0.0",
    "svelte": "^5.0.0",
    "svelte-check": "^3.8.0",
    "typescript": "^5.4.5",
    "vite": "^5.4.0"
  }
}
